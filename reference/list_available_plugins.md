# List available Vamp plugins for download

List available Vamp plugins for download

## Usage

``` r
list_available_plugins(platform = NULL)
```

## Arguments

- platform:

  Platform to filter by: "win32", "osx", or "linux" (default: current
  OS)

## Value

A data frame with plugin names, descriptions, and download URLs
